name: Maven Deploy
on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: Set up JDK 11
        uses: actions/setup-java@v1
        with:
          java-version: 11
      - name: Maven build
        run: mvn --file pom.xml package

  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - name: test java
        uses: actions/setup-java@v1
        with:
          java-version: 11
          distribution: 'adopt'
      - name: Test
        run: mvn --batch-mode test
  
  publish-artifact:
    runs-on: ubuntu-latest
    needs: [unit-test]
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v2
        with:
          java-version: '11'
          distribution: 'adopt'
      - name: Set up Maven Settings for deploy
        uses: s4u/maven-settings-action@v2.1.1
      - name: Publish package
        env:
          GITHUB_USERNAME: ${{ secrets.USERNAME }}
          GITHUB_TOKEN: ${{ secrets.TOKEN }}        
        run: mvn source:jar deploy -DuniqueVersion=false -Dmaven.source.useDefaultManifestFile=true -Dmaven.source.includePom=true -DaltDeploymentRepository='github::default::https://maven.pkg.github.com/Roggerbogota13/mavenprueba'
        
  Docker:
    name: push Docker image
    runs-on: ubuntu-latest
    needs: [deploy]
    permissions:
      packages: write
      contents: read
    steps:
      - name: Ckeck out repo
        uses: actions/checkout@v3
      - name: Log in to Docker Hub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.USER_DOCKER }}
          password: ${{ secrets.PASSWORD_DOCKER }}

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: .
          push: true
          tags: juandiaz9102/app:latest





    #name: push Docker image
    #runs-on: ubuntu-latest
    #needs: [deploy]
    #permissions:
      #packages: write
      #contents: read
    #steps:

      #- name: Ckeck out repo
       # uses: actions/checkout@v3

      #- name: Log in to Docker Hub
       # uses: docker/login-action@v2
       # with:
        #  username: ${{ secrets.USER_DOCKER }}
        #  password: ${{ secrets.PASSWORD_DOCKER }}

      #- name: Build and push
  
        #uses: docker/build-push-action@v3
       # with:
          #context: ./Dockerfile
          #push: true
          #tags: juandiaz9102/app:latest



        #run: docker run -d -t --name nginx nginx:alpine


        #run: docker build --tag go:test1 ./Dockerfile
      #- run: docker push go:tes1

      #- name: Build and push
        #uses: docker/build-push-action@v3
        #with:
          #context: .
          #push: true
          #tags: juandiaz9102/go:tes1
      
